@* @model JobVM

<div class="card shadow border-0 mt-4 mb-4">
    <div class="card-header bg-secondary bg-gradient text-light text-center py-4">
        <h2 class="text-primary text-uppercase mb-1">@Model.Job.Title</h2>
        <p class="text-primary fw-semibold mb-0">@Model.Job.Category?.CategoryName</p>
    </div>
    <div class="card-body">
        <div class="row mb-4">
            <div class="col-6 col-md-2 offset-lg-1 pb-1">
                <a asp-action="FindJob" class="btn btn-outline-primary bg-gradient fw-semibold btn-sm text-uppercase">
                    <small>Back to Home</small>
                </a>
            </div>
        </div>

        <div class="row">
            <div class="col-12 col-lg-10 offset-lg-1">
                <div class="mb-4">
                    <span class="badge bg-info text-dark">@Model.Job.RequiredSkills</span>
                </div>

                <div class="mb-4">
                    <p class="text-secondary lh-sm">@Html.Raw(Model.Job.Description)</p>
                </div>

                <div class="text-center">
                    <button type="button" class="btn btn-primary bg-gradient w-50 py-2 text-uppercase fw-semibold">
                        Contact With The Job Poster
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>
 *@